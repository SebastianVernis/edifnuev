# Cloudflare Pages Configuration
name = "chispartbuilding"
pages_build_output_dir = "./public"

# Production deployment (main branch)
[env.production]
compatibility_date = "2024-01-01"

# Variables
[env.production.vars]
NODE_ENV = "production"
API_URL = "https://edificio-admin.sebastianvernis.workers.dev"

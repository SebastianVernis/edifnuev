# Cloud Run Environment Variables Template
# Copy these values to Cloud Run environment configuration

# Application
NODE_ENV=production
PORT=8080

# Security
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECURE_STRING_IN_PRODUCTION

# Application URL (set to your Cloud Run service URL)
APP_URL=https://your-service-name-REGION.run.app

# SMTP Configuration for emails
# Option 1: Gmail with App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@edificio-admin.com

# Option 2: SendGrid (Recommended for production)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_FROM=noreply@yourdomain.com

# Option 3: Mailgun
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=postmaster@your-domain.mailgun.org
# SMTP_PASS=your-mailgun-password
# SMTP_FROM=noreply@yourdomain.com

# Database (if using external database in future)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Optional: Google Cloud Storage for uploads (future enhancement)
# GCS_BUCKET_NAME=your-bucket-name
# GCS_PROJECT_ID=your-project-id

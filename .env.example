# Edificio Admin - Configuración de Entorno

# ⚠️ NOTA IMPORTANTE PARA CLOUDFLARE WORKERS:
# En producción (Cloudflare Workers), las variables se configuran en:
# 1. wrangler.toml [vars] - Variables públicas (NODE_ENV, APP_URL, etc.)
# 2. `wrangler secret put NOMBRE` - Variables sensibles (JWT_SECRET, API keys, passwords)
# 
# Ver SETUP_SECRETS.md para instrucciones completas de configuración.

# Puerto del servidor (solo para desarrollo local con Node.js)
PORT=3001

# JWT Secret Key (cambiar en producción)
# En Cloudflare Workers: wrangler secret put JWT_SECRET
JWT_SECRET=edificio-admin-secret-key-2025

# URL de la aplicación
APP_URL=http://localhost:3001

# Configuración SMTP para envío de emails
# Recomendaciones por proveedor:

# --- Gmail ---
# 1. Habilitar "Acceso de apps menos seguras" o usar App Password
# 2. Configuración:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-contraseña-o-app-password
SMTP_FROM=noreply@edificio-admin.com

# --- Outlook/Hotmail ---
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@outlook.com
# SMTP_PASS=tu-contraseña
# SMTP_FROM=noreply@edificio-admin.com

# --- Yahoo ---
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=tu-email@yahoo.com
# SMTP_PASS=tu-contraseña
# SMTP_FROM=noreply@edificio-admin.com

# --- SendGrid (Recomendado para producción) ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=tu-sendgrid-api-key
# SMTP_FROM=noreply@edificio-admin.com
#
# Para Cloudflare Workers, configurar:
# wrangler secret put SENDGRID_API_KEY
# (Ver SETUP_SECRETS.md para guía completa)

# --- Mailgun ---
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=postmaster@tu-dominio.mailgun.org
# SMTP_PASS=tu-mailgun-password
# SMTP_FROM=noreply@edificio-admin.com

# Ambiente (development o production)
NODE_ENV=development
